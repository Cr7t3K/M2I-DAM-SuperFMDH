<div class="bg-white shadow-lg rounded-lg overflow-hidden relative">
    <div class="relative">
        <div class="carousel relative w-full h-56 overflow-hidden">
            <img src="{{ asset('images/fixtures/appart-1.jpg') }}" alt="" class="carousel-item absolute inset-0 w-full h-full transition-opacity duration-1000 bg-cover bg-center">
        </div>
    </div>
    <div class="p-4">
        <div class="flex justify-between items-center">
            <h3 class="listing-title text-gray-900 font-bold text-xl">{{ listing.title }} - {{ listing.propertyType.name }}</h3>
            <p class="text-gray-900 font-semibold">{{ listing.price }}</p>
        </div>
        <p class="text-gray-600">{{ listing.city }}, France</p>
        <p class="text-gray-500 mt-2">{{ listing.description }}</p>
        <div class="flex items-center justify-between mt-3">
            <button class="px-3 py-2 bg-blue-400 hover:bg-blue-500 text-white text-xs font-bold uppercase rounded">Contact</button>
            <a href="{{ path('app_listing_house_show', {'id': listing.id}) }}" class="px-3 py-2 bg-gray-300 hover:bg-gray-400 text-gray-800 text-xs font-bold uppercase rounded">Details</a>
        </div>
    </div>
</div>
